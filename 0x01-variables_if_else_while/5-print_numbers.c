#include < stdio.h >

/**
 * main - print 0 to 9
 *
 * Description: Uses only putchar
 *
 * Return: Always return 0
 */

int main(void)
{
	char n;

	for (n == 0, n <= 9, n++)
		putchar ("%d", n);
	putchar ("\n");
	return (0);
}
